# Code review — Chore/TechDebt-20260909-230534

> **This file is a work order, not a discussion.** If you're handed this file, fix the open `[ ]`
> findings directly and report what changed. Tick each `[x]` as you land it. Pause only for a genuinely
> irreversible or ambiguous finding: record its durable disposition, take the safe path, and keep going.

**Review status:** `complete`
**Reviewed up to commit:** `43fa6bf57ddf3d39ff55313f14c648383d60df93`  `(2026-09-09)`
**Security-reviewed up to commit:** `43fa6bf57ddf3d39ff55313f14c648383d60df93`
**Judgment:** `approved`

## Review pass — 2026-09-09 — full

**Candidate base:** `61b83c98c82a71fc21c9a14be804dc254dd08295`
**Candidate head:** `43fa6bf57ddf3d39ff55313f14c648383d60df93`
**Candidate branch:** `Chore/TechDebt-20260909-230534`
**Candidate scope:** `all`
**Candidate path-set:** `sha256:e702d287a8a09eafd83da694425100009e6403a04e5a37b1ae68c8c64e7c8520` `(6 paths)`
**Candidate bundle:** `C:/Users/tommy/AppData/Local/Temp/claude/C--Users-tommy-source-repos-Concertable/781c1276-de00-45ad-b279-86bb5eeb738b/scratchpad/review-bundle-Chore-TechDebt-20260909-230534`
**Candidate bundle identity:** `sha256:ada4af0d4e4b5ccfbf2866e346514b1365824d42385340e97b9a8ec488f2acd9`
**Work-order path:** `reviews/Chore-TechDebt-20260909-230534.md`
**Work-order mode:** `new`
**Pass judgment:** `approved`

### Findings

No findings.

Native general pass (correctness / reuse / simplification / efficiency / error-handling): the diff replaces
a `Func<ApplicationDto, Task<ApplicationResponse>>` built by a `switch (membership.Type)` in
`ApplicationController.GetById` with (a) one exhaustive `membershipType switch` in a new
`ApplicationDto.ToResponse(TenantType, BookingSummary?)` extension on the existing `ApplicationMappers`
class, (b) `IApplicationMapper.ToResponseAsync(dto, membershipType)` doing the shared booking fetch then
delegating, and (c) a controller that guards the no-membership case (`membership.Type is not { }
membershipType` → `Forbid()`) then forwards the caller's type. Behaviour is preserved for every reachable
input: `membership.Type` is `TenantType?` with only `Venue`/`Artist` defined, `null` ⟺ no active membership,
and the two response bodies are produced by the unchanged `dto.ToVenueResponse` / `dto.ToArtistResponse`.
The old `default:` arm and the new `_ => throw new ArgumentOutOfRangeException(...)` are both unreachable
today; the throw is the honest replacement (a future third `TenantType` faults loudly at the mapper rather
than silently 403-ing). `ToVenueResponseAsync` (singular) had no remaining caller and is removed from the
interface and impl together; `ToArtistResponseAsync` stays (used by `Apply`). `MapAsync` before the terminal
is required (the projection is async) and matches every other action in the file.

Routed rules re-checked against every changed file:
- `csharp-style` / `dotnet-standards:csharp-style` — file-scoped namespaces, no `_` prefix, no primary
  constructors, `this.`-only-to-disambiguate (no constructor changes in this diff), the C# 14
  `extension(ApplicationDto dto)` block reused rather than a legacy `this`-parameter method, one-line
  docstring on the new interface member, no `#region` outside the test file's per-operation grouping. The
  `is not { } membershipType` capture is the sanctioned form — `membership.Type` is a member access captured
  once to avoid repetition and the nullable-value `.Value`, exactly the standard's CORRECT example.
- `csharp-naming` / `dotnet-standards:csharp-naming` — `ToResponse` is a small closed-enum conversion in an
  `XMappers` extension (the standard's named case for an exhaustive switch); `ToResponseAsync` on the mapper
  interface mirrors the sibling `ToArtistResponseAsync`; no new type suffixes introduced.
- `http-api` / `dotnet-standards:http-api` / `dotnet:http-api` — no route change, no new `Request`/`Response`
  type; `ApplicationResponse` (Api-layer) is the existing role-shaped wire type, which the standard names as
  the legitimate reason for a dedicated `Response`; no `Tenant`→`organization` boundary touched.
- `result-terminals` / `dotnet-standards:result-terminals` / `dotnet:result-terminals` — one adapter
  namespace, `IError`-typed terminal with no problem mapper, `ToOkOrProblem` unchanged; the async projection
  has no sync-projected terminal overload so `MapAsync` stays.
- `integration-testing` / `dotnet-standards:integration-testing` / `dotnet:integration-testing` — the two
  new tests live in `ApplicationApiTests` under a `#region Get` (per-operation grouping), derive the client
  and application from `fixture.SeedState` (no invented literals), and each asserts an observable contract
  (status + `id` + the role-specific action key: venue → `accept`, artist → `withdraw`, and the absence of
  the other role's key) rather than a bare `OK`.
- `docs-and-debt` — the resolved `api/Concertable.B2B/TECH_DEBT.md` entry is deleted in full (not archived),
  one `---` separator retained between its former neighbours.

Module-boundary / test-impact lenses: every change is inside `Concertable.B2B.Application.Api` and its own
test project; `IApplicationMapper` stays `internal`, the extension calls its siblings in the same class, no
new project reference, no cross-module reach. Both rerouted branches (venue shape, artist shape) get direct
integration coverage; the `null → Forbid()` path is unreachable through the real permission stack
(`OperationsView` is only granted with a Venue/Artist membership) and was equally untested before.

Security pass (`ApplicationController.cs` matches the merge gate's `Controller[A-Za-z]*\.cs$`
`security_paths` entry): no findings. `[HasPermission(SharedPermissions.OperationsView)]` is unchanged; the
authorization guard is refactored to a behaviourally-equivalent form; response content, field set, and
`GetByIdAsync` tenant-scoping are identical to before. No new input, injection surface, secret handling, or
auth-token path. The unreachable `_ => throw` yields a generic 500 problem response, not an information leak.
