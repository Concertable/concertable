# Code review — Chore/TechDebt-20260910-001309

> **This file is a work order, not a discussion.** If you're handed this file, fix the open `[ ]`
> findings directly and report what changed. Tick each `[x]` as you land it. Pause only for a genuinely
> irreversible or ambiguous finding: record its durable disposition, take the safe path, and keep going.

**Review status:** `complete`
**Reviewed up to commit:** `5dbc63ca6a2487597d66fd71fec630bb64cd8660`  `(2026-09-10)`
**Judgment:** `approved`

## Review pass — 2026-09-10 — full

**Candidate base:** `61b83c98c82a71fc21c9a14be804dc254dd08295`
**Candidate head:** `5dbc63ca6a2487597d66fd71fec630bb64cd8660`
**Candidate branch:** `Chore/TechDebt-20260910-001309`
**Candidate scope:** `all`
**Candidate path-set:** `sha256:b3615b09ec22756d7d46179037a143645a4d06b0058730adbe69832627999fd2` `(5 paths)`
**Work-order path:** `reviews/Chore-TechDebt-20260910-001309.md`
**Work-order mode:** `new`
**Pass judgment:** `approved`

Change: `AdminService`'s admin-grant path hand-rolled a duplicate-key catch/discard instead of using the
shared `IUnitOfWork.TrySaveChangesAsync` boundary every other B2B module already uses for the same race
(e.g. `ApplicationWorkflow.ApplyAsync`). Adds a module-local `UnitOfWork<AdminDbContext>` following the
existing per-module pattern, registers it, routes both grant-save call sites through it, deletes the now-
redundant private method, updates the unit tests' mocks/assertions, and deletes the resolved
`api/Concertable.DataAccess/TECH_DEBT.md` entry. Resolves the "Standardize the duplicate-aware save"
tech-debt item.

Layers exercised: native-general (correctness / reuse / simplification / efficiency / error handling), plus
parent re-check against the routed skills — `csharp-style`, `csharp-naming`, `dotnet-standards:persistence`,
`dotnet:persistence`, `dotnet-standards:dependency-injection`, `dotnet-standards:unit-testing`,
`docs-and-debt`. No path in this diff matches the merge gate's `security_paths` inventory
(`Concertable.Auth`, `Concertable.Payment`, `.Contracts`, `Controller*.cs`), so no security pass was run and
no security marker is recorded.

### Findings

No findings. Confirmed equivalent behavior: the shared `DbContext.TrySaveChangesAsync(Func<DbUpdateException,
bool>, CancellationToken)` extension's `ChangeTracker.Clear()` reproduces the old
`DbUpdateException.DiscardFailedChanges()` path for this duplicate-key race — both leave the tracker clean
for the next `/api/auth/me` retry. The race-condition comment that documented the old private method was
moved intact onto `EnsureCurrentUserAdminGrantedIfEligibleAsync` rather than lost. Both call sites were
updated symmetrically (no half-migration). `UnitOfWork<AdminDbContext>` matches the existing sibling-module
shape (Application/Booking/Concert/Opportunity) and is registered scoped, matching `AddDbContext`'s default
scoped `AdminDbContext` lifetime, so the unit-of-work and the repositories share the same context instance
per request — no lifetime mismatch. Tests were updated in lockstep: mocks retargeted from
`invitationRepository.SaveChangesAsync` to `unitOfWork.TrySaveChangesAsync`, preserving the same
success/duplicate-key-no-op/unexpected-exception-propagation assertions. The deleted `TECH_DEBT.md` entry is
exactly the one this PR resolves; the file's other entry is untouched. Build clean across
`Concertable.B2B.Admin.Infrastructure`/`.Api`/`.UnitTests`/`.IntegrationTests`; 33/33 unit tests pass.
